import React from "react";

const LoadingSpinner = () => {
	return (
		<div className="text-center pt-40 ">
			<i className="fas fa-circle-notch text-4xl text-primary animate-spin"></i>
		</div>
	);
};

export default LoadingSpinner;
